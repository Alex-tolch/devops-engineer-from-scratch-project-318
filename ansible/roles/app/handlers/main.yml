---
- name: Restart bulletin stack
  community.docker.docker_compose_v2:
    project_src: "{{ app_deploy_dir }}"
    state: restarted

- name: Restart nginx metrics proxy
  ansible.builtin.service:
    name: nginx
    state: restarted
