services:
  app:
    image: {{ docker_image }}
    env_file:
      - .env
    ports:
      - "8080:8080"
      - "127.0.0.1:{{ metrics_actuator_host_port | default(9091) }}:9090"
    restart: unless-stopped
    networks:
      - bulletin-net

networks:
  bulletin-net:
    name: bulletin-net
