---
# CI has no ansible/.vault-password; syntax-check cannot decrypt group_vars/app/vault.yml.
profile: basic
exclude_paths:
  - .cache/
  - molecule/
  - group_vars/**/vault.yml
skip_list:
  - var-naming[no-role-prefix]
  - internal-error
warn_list:
  - schema
  - name
  - role-name
