# Grafana assets

PNG captures from the live Grafana UI and ntfy (for README / assignment evidence). Re-capture after dashboard or alerting changes.

| File | Source |
|------|--------|
| `system-resources.png`, `application-health.png`, `http-traffic.png` | Bulletins dashboards at http://165.232.72.139:3000 |
| `alert-ntfy.png` | ntfy client after `make grafana-test-alert` |

Login: `admin`, password `vault_grafana_admin_password` in encrypted `ansible/group_vars/app/vault.yml`.
